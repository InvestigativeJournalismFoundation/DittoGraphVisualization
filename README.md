## How to run
-> drop a csv of clusters into /public/clusters.csv, with cols ["name", "cluster_id"], a csv of edges into /public/edges.csv with cols ["name1", "name2", "score"]

```bash
cd viewer
npm i
npm run dev
```
